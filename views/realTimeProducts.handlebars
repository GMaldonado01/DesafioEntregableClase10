{{#each  products}}
<h2>{{this.title}} </h2>
<p> {{this.description}} </p>
<p>{{this.price}}</p>
{{/each}} 
<script src="/socket.io/socket.io.js"></script> 

<script> 
    const socket = io();
</script>

<script>
    const socketClient = io()
socketClient.on("updateProducts", allProducts => {
        console.log({allProducts})
    })
</script>
